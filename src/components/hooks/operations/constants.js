/**
 * Shared numeric defaults for spatial sampling and smoothing.
 * @module operations/constants
 */

/** Default grid step in decimal degrees for interior point grids (`generateInteriorPointGrid`). */
export const DEFAULT_INTERIOR_GRID_STEP = 0.0003;

/** Default Chaikin smoothing iterations in `smoothPolygonChaikin`. */
export const DEFAULT_CHAIKIN_ITERATIONS = 5;
